requests>=2.31
beautifulsoup4>=4.12
markdownify>=0.13
PyYAML>=6.0
lxml>=5.0
trafilatura>=1.8
markdown>=3.5

# Optional — only needed when enrich.enabled is true (TL;DR + FAQ generation).
# anthropic>=0.40
